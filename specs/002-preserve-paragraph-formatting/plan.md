# Implementation Plan: Preserve Paragraph Layout in Translation

**Branch**: `002-preserve-paragraph-formatting` | **Date**: 2026-08-18 | **Spec**: [specs/002-preserve-paragraph-formatting/spec.md](spec.md)

**Input**: Feature specification from `/specs/002-preserve-paragraph-formatting/spec.md`

## Summary

Bảo tồn 100% bố cục và phân đoạn của văn bản gốc trong quá trình dịch thuật AI (Giai đoạn 1 Dịch thô & Giai đoạn 2 Chuốt văn). Ngăn chặn triệt để hiện tượng AI nén toàn bộ chương truyện thành một đoạn văn duy nhất hoặc dồn câu mở đầu vào cùng dòng với tiêu đề chương.

## Technical Context

**Language/Version**: TypeScript 5.8+, Node.js 22+, React 19, Express.js

**Primary Dependencies**: `@google/genai`, Vitest

**Storage**: IndexedDB (`src/services/db.ts`)

**Testing**: `vitest`

**Target Platform**: Fullstack (Frontend React + Backend Express)

**Project Type**: Web Application

**Performance Goals**: Không làm tăng độ trễ dịch thuật (0ms overhead)

**Constraints**: Tuân thủ nghiêm ngặt 5 nguyên tắc của Hiến pháp dự án (Constitution).

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Principle I (Strict Quality Gates)**: ✅ Phải pass `tsc --noEmit`, `vitest run`, `npm run build`.
- **Principle II (Dependency Minimization)**: ✅ Không thêm package ngoài.
- **Principle III (Strict Concern Separation)**: ✅ Cập nhật Prompt và Hậu xử lý cho đúng tầng backend và frontend utils.
- **Principle IV (Immutable Core Schemas)**: ✅ Giữ nguyên cấu trúc `Chapter` và IndexedDB.
- **Principle V (Atomic Commits)**: ✅ Phạm vi thay đổi giới hạn và chính xác.

## Project Structure

### Documentation (this feature)

```text
specs/002-preserve-paragraph-formatting/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output
│   └── text-cleaner.contract.md
└── tasks.md             # Phase 2 output (generated by /speckit-tasks)
```

### Source Code Touchpoints

```text
server/
├── utils/
│   └── text.ts                               # [MODIFY] Thêm quy tắc phân đoạn vào LITERARY_TRANSLATION_FRAMING & separateChapterTitleAndBody
├── controllers/
│   └── translation/
│       ├── rawController.ts                  # [MODIFY] Thêm chỉ thị phân đoạn vào prompt dịch thô & hậu xử lý
│       └── polishController.ts               # [MODIFY] Thêm chỉ thị phân đoạn vào prompt chuốt văn & hậu xử lý

src/
├── utils/
│   ├── textCleaner.ts                        # [MODIFY] Thêm separateChapterTitleAndBody ở frontend
│   └── __tests__/
│       └── textCleaner.test.ts               # [NEW] Unit tests kiểm thử bộ tách tiêu đề và phân đoạn
└── services/
    └── chapterTranslationService.ts          # [MODIFY] Áp dụng chuẩn hóa trước khi lưu vào IndexedDB
```

## Complexity Tracking

Không có vi phạm nguyên tắc Constitution nào.
